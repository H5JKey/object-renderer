name: C++ checks

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]



jobs:
  style:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Run clang-format check
      uses: mrousavy/clang-format-action@v1
      with:
          clang-format-version: '18'
          check-path: 'renderer'
          clang-format-style-path: renderer/.clang-format

  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Build and test
      run: |
        docker build . --file renderer/Dockerfile --tag renderer
        docker run --rm renderer sh -c "cd build/tests && ctest --output-on-failure"
